---
doc_type: figma_index
title: v0.1 Figma ファイル一覧（主案 + バックアップ案）
product: freoli_web_platform
as_of: 2026-05-18
owners: [endo]
status: template
parent_flow: docs/design/ui_style_confirmation_flow.md
---

# v0.1 Figma リンク集

> Claude Design からの出力を Figma で受け取った後、URL をここに記録する。
> Stage 2（Day 4）の主案選定完了時点で確定させる。

---

## 主案（Primary）

- **アー写撮影完了前提**：5/24〜25 に撮影予定のアー写を使用
- **Figma URL**：`(未記入)` ← Claude Design 出力 URL を貼る
- **Figma Pages**：
  - `Cover` — 表紙・案の総覧
  - `Hero / 320px / 640px / 1024px / 1536px`
  - `NextLive / 320px / 640px / 1024px / 1536px`
  - `ContactForm / Normal / Focus / Error / Success`
  - `Members / Card variants`
  - `Tokens` — Figma Variables（`cyan-400` 等の Tailwind 標準名で登録）
- **採用した発散軸**：`(例：A-a × B-a × C-c)` ← 主案を選んだ軸の組み合わせ
- **キャプション**：`(主案の一言コンセプト)`

---

## バックアップ案（Fallback）

- **既存ライブ写真 10 枚 から選定する前提**：アー写撮影が 5/28 以降にずれた場合の代替
- **Figma URL**：`(未記入)`
- **採用した発散軸**：`(例：A-c × B-c × C-a)`
- **キャプション**：`(バックアップ案の一言コンセプト)`
- **主案との差分**：
  - 例：ヒーローはバンド写真フル幅ではなくテキスト中心構図
  - 例：ネオン光は点光源ではなくアウトライン表現
  - 例：（差分は主案選定後に追記）

---

## 共有設定

- **共有範囲**：プロジェクトメンバー全員（えんまさ / レビュアー）に view 権限
- **コメント可否**：Stage 2 レビュー期間中は comment 権限を付与、Stage 3 以降は view のみ
- **Figma プラグイン**：`Figma to Code (Tailwind)` 等の利用は禁止 — `design_spec.md` の手動転記で精度を担保する

---

## 補足リンク

- 参考にしたサウンドリファレンスのアートワーク URL
  - サカナクション：`(URL)`
  - BUMP OF CHICKEN：`(URL)`
  - PEOPLE 1：`(URL)`
  - チョーキューメイ：`(URL)`
- インスピレーション元の他バンド公式サイト（あれば）
  - `(URL)`

---

## 更新履歴

| 日付 | 更新内容 | 担当 |
|---|---|---|
| `(YYYY-MM-DD)` | 初版作成（Claude Design 発散完了時） | endo |
| `(YYYY-MM-DD)` | 主案 + バックアップ案 確定（Stage 2 完了時） | endo |
