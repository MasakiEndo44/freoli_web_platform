---
doc_type: selection_log
title: v0.1 デザイン主案 + バックアップ案 選定ログ
product: freoli_web_platform
as_of: 2026-05-18
owners: [endo]
status: template
parent_flow: docs/design/ui_style_confirmation_flow.md
milestone: MS①（Stage 2 完了）
---

# v0.1 デザイン選定ログ

> Stage 2（Day 4）の主案 + バックアップ案 選定時に記入する。
> North Star Quote 含意 5 項目の評価表 + えんまさ承認ログを残す。

---

## 1. 選定セッション情報

| 項目 | 内容 |
|---|---|
| 開催日 | `(YYYY-MM-DD)` |
| レビュアー | えんまさ（Content Owner / Tech Owner） |
| 想定ユーザーヒアリング | `(氏名 or イニシャル)` — Primary Persona 相当の IT 系デザイナー 15 分聞き取り |
| 発散案数 | `(例：8 案)` |
| 検討時間 | `(例：90 分)` |

---

## 2. 評価対象案一覧

| 案 ID | 採用軸 (A×B×C) | キャプション | 主案候補 / バックアップ候補 |
|---|---|---|---|
| 案 1 | `A-a × B-a × C-c` | バンド写真フル幅 × 点光源 × 混在タイポ | `(候補種別)` |
| 案 2 | `(A×B×C)` | `(キャプション)` | — |
| 案 3 | `(A×B×C)` | `(キャプション)` | — |
| 案 4 | `(A×B×C)` | `(キャプション)` | — |
| 案 5 | `(A×B×C)` | `(キャプション)` | — |
| 案 6 | `(A×B×C)` | `(キャプション)` | — |
| 案 7 | `(A×B×C)` | `(キャプション)` | — |
| 案 8 | `(A×B×C)` | `(キャプション)` | — |
| 案 9 | `(A×B×C)` | `(キャプション)` | — |

---

## 3. North Star Quote 含意 5 項目評価表

> 各案に対し、5 項目を ⚪︎（満たす）/ △（部分的に満たす）/ ✗（満たさない）で評価。

| 案 ID | ① 日付・会場が一瞬で分かる | ② 予約まで終わる | ③ 色合いが好み | ④ 心が先に動く | ⑤ "あの色のバンド" | 合計 ⚪︎ |
|---|---|---|---|---|---|---|
| 案 1 | `(⚪︎/△/✗)` | `(⚪︎/△/✗)` | `(⚪︎/△/✗)` | `(⚪︎/△/✗)` | `(⚪︎/△/✗)` | `(N)` |
| 案 2 | | | | | | |
| 案 3 | | | | | | |
| 案 4 | | | | | | |
| 案 5 | | | | | | |
| 案 6 | | | | | | |
| 案 7 | | | | | | |
| 案 8 | | | | | | |
| 案 9 | | | | | | |

### 3.1 評価の所見（自由記述）

- 全体傾向：`(例：軸 B-a 点光源系の案が全体的に "あの色のバンド" 印象を強く与える)`
- 想定ユーザーヒアリングの主なフィードバック：`(例：「日本語キャッチが大きい案の方が読みやすい」)`
- 議論で出た反論や懸念：`(例：「グラデーション案はインディーらしさが薄れる」)`

---

## 4. 主案（Primary）選定

### 4.1 採用案

- **案 ID**：`(例：案 3)`
- **採用軸**：`(A-? × B-? × C-?)`
- **キャプション**：`(主案の一言コンセプト)`
- **想定素材**：アー写（5/24〜25 撮影予定）

### 4.2 採用理由（North Star 5 項目との対応）

| 含意 | 主案がどう満たすか |
|---|---|
| ① 日付・会場が一瞬で分かる | `(具体的にどの設計要素で実現するか)` |
| ② 予約まで終わる | `(同上)` |
| ③ 色合いが好み | `(同上)` |
| ④ 心が先に動く | `(同上)` |
| ⑤ "あの色のバンド" | `(同上)` |

### 4.3 不採用案との差分

- `(例：案 5 ではなく案 3 を選んだ理由 — ヒーローの情報密度が低く、Persona の "一瞬で分かる" を満たしやすい)`

---

## 5. バックアップ案（Fallback）選定

### 5.1 採用案

- **案 ID**：`(例：案 7)`
- **採用軸**：`(A-? × B-? × C-?)`
- **キャプション**：`(バックアップ案の一言コンセプト)`
- **想定素材**：既存ライブ写真 10 枚 から選定（アー写撮影が 5/28 以降に遅延した場合）

### 5.2 主案との差分

| 観点 | 主案 | バックアップ案 |
|---|---|---|
| ヒーロー構図 | `(主案の軸 A)` | `(バックアップ案の軸 A)` |
| 光表現 | `(主案の軸 B)` | `(バックアップ案の軸 B)` |
| タイポ主張 | `(主案の軸 C)` | `(バックアップ案の軸 C)` |
| 想定素材の質感 | スタジオ撮影・整った構図 | ライブ会場・観客込み・暗所 |
| Tailwind config 影響 | `(色トークン共通か差分か)` | `(同上)` |

### 5.3 バックアップ案発動条件

- アー写撮影が **5/26 朝時点で完了していない** 場合、バックアップ案に切り替え判断
- 5/27 朝時点でも未完了の場合、バックアップ案で **Stage 3 開始**

---

## 6. 不採用案の保管

- 不採用案 7 案分も Figma 上に保持（削除しない）
- v0.5 以降のリブランディング検討時に再評価素材として活用可能

---

## 7. えんまさ承認

- **承認日**：`(YYYY-MM-DD)`
- **承認方法**：`(Git コミットメッセージ / Slack / Figma コメント のいずれか)`
- **承認文言**：`(例：「主案 = 案 3、バックアップ = 案 7 で確定。Stage 3 進行 OK」)`
- **コミット SHA**：`(承認時の commit hash)`

---

## 8. 次ステップへの引き渡し

選定確定後、以下に着手：

1. [ ] `design_spec.md` の `(主案で確定)` プレースホルダーを主案の数値で埋める
2. [ ] `figma_links.md` の URL を確定値で更新
3. [ ] Stage 3 開始（`tailwind.config.ts` へのトークン反映）
4. [ ] Stage 4 開始（5 プリミティブ実装 + `/styleguide` 構築）

---

## 9. 更新履歴

| 日付 | 更新内容 | 担当 |
|---|---|---|
| `(YYYY-MM-DD)` | 初版作成（テンプレート配置時） | endo |
| `(YYYY-MM-DD)` | Stage 2 選定セッション結果を記入 | endo |
| `(YYYY-MM-DD)` | バックアップ案発動 / 不発動の最終判定 | endo |
